require "spec_helper"

describe NopioScraper do
  it "has a version number" do
    expect(NopioScraper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
